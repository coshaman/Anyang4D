# Anyang response-resource model

Municipal emergency-water records expose `capacity_tons_per_day` and `available_persons` only as `RESPONSE_RESOURCE_CAPACITY`. They are not shelter capacity, are not used to assign evacuees, and do not authorize dispatch optimization. Flood-response inventory is a 33-row municipal context dataset and is likewise not automatically dispatched.
