# Competition demo flow

Open `/admin?demo=1`. The demo selects the `GENERAL_EVACUATION` competition preset, shows the timeline, affected demand, participation assumption, assignments, changed roads, available shelters, and bottleneck candidates. In demo mode the A/B control is preselected to the persisted fixed variant `anyang-general-evacuation-competition-shelter-outage`, which keeps the same area and closes one high-load shelter. The measured delta is an exact result: available shelters `-1`, assignment cost `+1,001,955.9 m`, with the frozen graph/area producing no assigned/unserved delta in that run.

The provenance panel is represented by the frame labels and source manifest: national filtered records are `NATIONAL_OFFICIAL_FILTERED_ANYANG`, municipal extracts are `ANYANG_LOCAL_OFFICIAL`, population totals are official municipal totals with simulated spatial anchors, and hazard geometry is `ADMIN_SCENARIO`.

Goal 5A demo extension: choose `/admin?demo=1`, open “AI 대규모 시나리오 선별”, generate 100 simulated candidates, inspect AI estimates/support state, then compare the exact-verified top-K. The exact reference result is the displayed authoritative value; the AI estimate remains separately auditable. The UI explains the sequence and expected CPU wait while the request runs, so the delay is not presented as unexplained live inference. Current decision is `AI_SURROGATE_B` / `ADMIN_AI_SCENARIO_SCREENING = DEMO_ONLY`. The conservative representative real-browser timing is 24.889 seconds for 100 candidates plus exact verification on the local Windows CPU run; a separate latest warm repetition was 12.408 seconds and is not the headline claim.
