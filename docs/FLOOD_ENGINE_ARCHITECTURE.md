# Flood engine boundary after Goal 3D

Goal 3D supplies no flood simulation input authorization. A future flood engine must consume a separately authorized, quality-approved terrain artifact and independently validate hydrology, boundary conditions, rainfall/scenario assumptions, numerical stability, and hazard claims. The current `TERRAIN_C` artifact is diagnostic only.

